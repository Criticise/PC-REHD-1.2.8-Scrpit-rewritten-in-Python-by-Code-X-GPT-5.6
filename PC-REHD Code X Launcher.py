# PC-REHD Code X legacy update compatibility marker
#
# This root-level Blob intentionally is not an executable Launcher. It exists
# only for pre-relocation builds that compare the historical root source URL.
# Those builds will detect the changed marker and direct users to the release.
legacy_update_marker=pc-rehd-code-x-v1.0.0
legacy_source_layout=relocated
